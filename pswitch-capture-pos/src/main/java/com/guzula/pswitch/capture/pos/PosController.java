package com.guzula.pswitch.capture.pos;

/**
 * Referência: pos.controller.ts (guzula-switch). TODO: avaliar se faz sentido como @RestController
 * (inspeção/admin) já que o canal real é TCP raw, não HTTP.
 */
public class PosController {}
